# API Reference

## Classes

| Name | Description |
|------|-------------|
| [`Logger`](#logger-1) |  |

## Macros

---

### STANDARDLOOP_LOGGER_H_MAJOR_VERSION

```cpp
#define STANDARDLOOP_LOGGER_H_MAJOR_VERSION 0
```

Defined in logger.h:9

---

### STANDARDLOOP_LOGGER_H_MINOR_VERSION

```cpp
#define STANDARDLOOP_LOGGER_H_MINOR_VERSION 0
```

Defined in logger.h:10

---

### STANDARDLOOP_LOGGER_H_PATCH_VERSION

```cpp
#define STANDARDLOOP_LOGGER_H_PATCH_VERSION 17
```

Defined in logger.h:11

---

### STANDARDLOOP_LOGGER_H_VERSION

```cpp
#define STANDARDLOOP_LOGGER_H_VERSION "0.0.17"
```

Defined in logger.h:12

## Enumerations

---

### LogLevel

```cpp
enum LogLevel
```

Defined in logger.h:21

| Value | Description |
|-------|-------------|
| `TRACE` |  |
| `DEBUG` |  |
| `INFO` |  |
| `WARN` |  |
| `ERROR` |  |
| `FATAL` |  |

---

### LogType

```cpp
enum LogType
```

Defined in logger.h:31

| Value | Description |
|-------|-------------|
| `STANDARD_FMT` |  |
| `JSON_FMT` |  |
## Typedefs

---

### Logger

```cpp
using Logger = struct Logger
```

Defined in logger.h:45

## Functions

---

### InitLogger

```cpp
void InitLogger(enum LogLevel level, enum LogType type, bool timestamp, bool flush, bool newline, bool color)
```

Defined in logger.h:56

Initializes the [Logger](#logger-1).

#### Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `level` | `enum LogLevel` | The log level from the LogLevel enum. |
| `type` | `enum LogType` | The log type from the LogType enum. |
| `timestamp` | `bool` | Turn on or off timestamps in logs. |
| `flush` | `bool` | If true, fflush(stderr) will be called. |
| `newline` | `bool` | If true, a newline will be inserted. |
| `color` | `bool` | If true, color output will be enabled. |

---

### InitLoggerEasy

```cpp
void InitLoggerEasy(enum LogLevel)
```

Defined in logger.h:58

---

### PrintLoggerConfig

```cpp
void PrintLoggerConfig(void)
```

Defined in logger.h:59

---

### Log

```cpp
void Log(enum LogLevel, const char *, ...)
```

Defined in logger.h:61

---

### Panic

```cpp
void Panic(const char *, ...)
```

Defined in logger.h:62

---

### SetLogLevel

```cpp
void SetLogLevel(enum LogLevel)
```

Defined in logger.h:63

---

### StringToLogLevel

```cpp
enum LogLevel StringToLogLevel(const char *)
```

Defined in logger.h:64

---

### GetLogLevel

```cpp
enum LogLevel GetLogLevel()
```

Defined in logger.h:66

---

### LogLevelToString

```cpp
char * LogLevelToString(enum LogLevel)
```

Defined in logger.h:67



## Logger

```cpp
struct Logger
```

Defined in logger.h:37

### Public Attributes

| Return | Name | Description |
|--------|------|-------------|
| `enum LogLevel` | [`log_level`](#log_level)  |  |
| `enum LogType` | [`log_type`](#log_type)  |  |
| `bool` | [`timestamp`](#timestamp)  |  |
| `bool` | [`flush`](#flush)  |  |
| `bool` | [`newline`](#newline)  |  |
| `bool` | [`color`](#color)  |  |

---

#### log_level

```cpp
enum LogLevel log_level
```

Defined in logger.h:39

---

#### log_type

```cpp
enum LogType log_type
```

Defined in logger.h:40

---

#### timestamp

```cpp
bool timestamp
```

Defined in logger.h:41

---

#### flush

```cpp
bool flush
```

Defined in logger.h:42

---

#### newline

```cpp
bool newline
```

Defined in logger.h:43

---

#### color

```cpp
bool color
```

Defined in logger.h:44

Generated by [Moxygen](https://0state.com/moxygen)